import 'package:flutter/material.dart';

class GrapeListPage extends StatelessWidget {
  const GrapeListPage({super.key});

  static const path = '/grapes';

  @override
  Widget build(BuildContext context) => const Placeholder();
}
